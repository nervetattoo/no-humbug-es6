'use strict'

/* Write a small program that lists images from a provided NASA API in a browser,
* and in turn lets you post those images to a provided Slack channel through
* the incoming webhook URL provided.
*
* The solution should be in a variable named `App` and it will be started like this:
* solution = new App({ container, apiUrl, slackUrl })
* solution.render()
*
*/

module.exports = App
